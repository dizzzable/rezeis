# Реальный Caddyfile — генерится из Caddyfile.example и содержит домен оператора
Caddyfile

# На случай, если кто-то положит сертификаты прямо сюда (не рекомендуется —
# держим их в /etc/acme).
*.pem
*.key
