import { RequestMetadataInterface } from './request-metadata.interface';

/**
 * Describes normalized metadata for an internal admin request.
 */
export interface InternalAdminRequest extends RequestMetadataInterface {}
