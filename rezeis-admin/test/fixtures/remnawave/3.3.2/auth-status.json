{
  "version": "3.3.2",
  "endpoint": "GET /api/auth/status",
  "source": "derived mechanically from the success response of GET /api/auth/status in the vendor OpenAPI document \"Remnawave API v3.3.2.json\" (info.version 3.3.2). Key set and key ORDER are the specification's; every nullable slot is populated. `body` is the COMPLETE HTTP body, envelope included.",
  "specRequired": [
    "isLoginAllowed",
    "isRegisterAllowed",
    "authentication",
    "branding"
  ],
  "body": {
    "response": {
      "isLoginAllowed": true,
      "isRegisterAllowed": true,
      "authentication": {
        "passkey": {
          "enabled": true
        },
        "oauth2": {
          "providers": {
            "telegram": true,
            "github": true,
            "pocketid": true,
            "yandex": true,
            "keycloak": true,
            "generic": true
          }
        },
        "password": {
          "enabled": true
        }
      },
      "branding": {
        "title": "title-0",
        "logoUrl": "logoUrl-0"
      }
    }
  }
}
