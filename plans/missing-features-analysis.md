# Анализ отсутствующих функций в altshop-panel

## Обзор

Данный документ содержит анализ функций, присутствующих в Telegram-боте `altshop`, но отсутствующих в веб-панели `altshop-panel`. На основе этого анализа можно определить приоритеты внедрения недостающего функционала.

---

## 1. Платежные шлюзы (Payment Gateways)

### 1.1. Список поддерживаемых шлюзов в боте

В боте реализовано **7 платежных шлюзов**:

| Шлюз | Тип | Настройки | Статус |
|------|-----|-----------|--------|
| **Cryptopay** | Криптовалюта | `api_key` (SecretStr) | Активен |
| **Heleket** | Карты/Крипта | `api_key` (SecretStr) | Активен |
| **Pal24 (PayPalych)** | Карты | `api_key`, `shop_id` | Активен |
| **Platega** | Карты | `merchant_id`, `secret`, `payment_method` | Активен |
| **WATA** | Карты | `access_token`, `base_url` | Активен |
| **YooKassa** | Карты | `shop_id`, `api_key`, `customer`, `vat_code` | Активен |
| **Telegram Stars** | Внутренняя валюта TG | Нет настроек | Активен |

### 1.2. Архитектура шлюзов

```python
# Базовый класс в altshop/src/infrastructure/payment_gateways/base.py
class BasePaymentGateway(ABC):
    @abstractmethod
    async def handle_create_payment(self, amount: Decimal, details: str) -> PaymentResult:
        """Создание платежа"""
        
    @abstractmethod
    async def handle_webhook(self, request: Request) -> tuple[UUID, TransactionStatus]:
        """Обработка webhook от платежной системы"""
```

### 1.3. Настройка шлюзов в админке бота

**Расположение:** `altshop/src/bot/routers/dashboard/rezeis/gateways/`

Администратор может:

1. **Просматривать список шлюзов**
   - Отображение всех 7 шлюзов со статусом (вкл/выкл)
   - Индикатор настроенности (зеленая/красная точка)

2. **Настраивать каждый шлюз**
   - Динамическая форма на основе `GatewaySettingsDto`
   - Поля зависят от типа шлюза:
     - YooKassa: shop_id, api_key, customer, vat_code
     - Cryptopay: api_key
     - Pal24: api_key, shop_id
     - Platega: merchant_id, secret, payment_method
     - WATA: access_token, base_url
     - Heleket: api_key
     - Telegram Stars: нет полей

3. **Управлять активностью**
   - Включение/выключение шлюза
   - Тестовый платеж для проверки работоспособности

4. **Управлять отображением**
   - Изменение порядка отображения (order_index)
   - Установка валюты по умолчанию для каждого шлюза

5. **Webhook URL**
   - Автоматическое формирование URL для webhook
   - Копирование URL в буфер обмена

### 1.4. Безопасность webhook

- **YooKassa**: Проверка IP-адреса (whitelist)
- **Heleket**: Проверка подписи запроса
- **Pal24**: Проверка подписи запроса
- **Cryptopay**: Проверка подписи
- **Telegram Stars**: Верификация через Bot API

### 1.5. Дополнительные шлюзы (возможные)

В DTO определены но не полностью реализованы:
- **Yoomoney**: wallet_id, secret_key
- **Cryptomus**: merchant_id, api_key
- **Robokassa**: shop_id, api_key, secret_key

---

## 2. Система локализации (i18n)

### 2.1. Формат и структура

**Библиотека:** Fluent (Project Fluent by Mozilla)
**Python-обертка:** fluentogram

**Расположение файлов:**
```
altshop/assets/translations/
├── en/
│   ├── buttons.ftl      (720 строк)
│   ├── messages.ftl     (2158 строк)
│   ├── notifications.ftl (608 строк)
│   └── utils.ftl
└── ru/
    ├── buttons.ftl      (733 строки)
    ├── messages.ftl     (2339 строк)
    ├── notifications.ftl (636 строк)
    └── utils.ftl
```

### 2.2. Поддерживаемые языки

- **English** (en)
- **Russian** (ru)

### 2.3. Структура переводов

#### Категории переводов:

1. **buttons.ftl** - Тексты кнопок
   - Навигация (back, main-menu)
   - Меню (connect, subscription, invite)
   - Админ-панель (dashboard, users, broadcast)
   - Платежи (gateways, plans)
   - Партнерка (partner menu)
   - Промокоды
   - Настройки

2. **messages.ftl** - Сообщения диалогов
   - Главное меню
   - Информация о подписке
   - Приглашение друзей
   - Реферальные награды
   - Статистика
   - Настройки партнерки
   - Конфигуратор планов
   - И многое другое

3. **notifications.ftl** - Уведомления
   - Ошибки
   - События (events)
   - Статусы операций
   - Успешные действия

### 2.4. Особенности Fluent

```ftl
# Пример с условиями и переменными
btn-user-partner-toggle = { $is_active ->
    [1] 🔴 Деактивировать
    *[0] 🟢 Активировать
}

# Пример с плюрализацией
msg-menu-invite = 
    Вы пригласили { $referrals } { $referrals ->
        [one] друга
        [few] друзей
        *[other] друзей
    }
```

### 2.5. Ключи локализации

Централизованное хранилище ключей:
```python
# altshop/src/core/i18n/keys.py
class I18nKeys(Enum):
    BTN_BACK = "btn-back"
    BTN_MAIN_MENU = "btn-main-menu"
    MSG_MAIN_MENU = "msg-main-menu"
    # ... и т.д.
```

### 2.6. Использование в коде

```python
from fluentogram import TranslatorRunner

i18n: TranslatorRunner

# Простой перевод
text = i18n.get("btn-back")

# С параметрами
text = i18n.get("msg-menu-invite", referrals=5)

# Вложенные ключи
text = i18n.get("msg-main-menu", status="ACTIVE")
```

---

## 3. Скрытая партнерская программа

### 3.1. Принцип работы "скрытости"

**Ключевая особенность:** Партнерская программа НЕ доступна по умолчанию всем пользователям.

**Механизм активации:**
1. Пользователь НЕ видит меню "Партнерка" до активации
2. Только **администратор** может выдать партнерский доступ
3. Админ выдает доступ через: `Панель управления → Пользователи → [Пользователь] → Партнерка → Выдать партнерку`

### 3.2. Кто может активировать партнерку

**Только администраторы** (роль ADMIN или SUPER_ADMIN)

**Путь в админке бота:**
```
Dashboard → Users → Search User → Select User → btn-user-partner → btn-user-partner-create
```

### 3.3. Условия и настройки партнерки

#### Глобальные настройки (доступны админу):

| Параметр | Описание | Тип |
|----------|----------|-----|
| `enabled` | Включена/выключена программа | bool |
| `level1_percent` | Процент с 1-го уровня рефералов | float |
| `level2_percent` | Процент со 2-го уровня рефералов | float |
| `level3_percent` | Процент с 3-го уровня рефералов | float |
| `tax_percent` | Налоговый вычет | float |
| `min_withdrawal_amount` | Минимальная сумма вывода (в копейках) | int |
| `yookassa_commission` | Комиссия YooKassa | float |
| `telegram_stars_commission` | Комиссия Telegram Stars | float |
| `cryptopay_commission` | Комиссия Cryptopay | float |
| `heleket_commission` | Комиссия Heleket | float |
| `pal24_commission` | Комиссия Pal24 | float |
| `wata_commission` | Комиссия WATA | float |
| `platega_commission` | Комиссия Platega | float |

#### Многоуровневая система (3 уровня):

```
Уровень 1: Прямые рефералы (приглашенные самим партнером)
Уровень 2: Рефералы рефералов (2-я линия)  
Уровень 3: 3-я линия рефералов
```

#### Расчет вознаграждения:

```
1. Сумма платежа реферала
2. Минус комиссия платежной системы
3. Минус налог (tax_percent)
4. От остатка начисляется % партнеру (в зависимости от уровня)
```

### 3.4. Интерфейс клиента (после активации)

После активации партнеру становится доступно меню **"👾 Партнерка"** с разделами:

1. **Главный экран**
   - Баланс (доступно к выводу)
   - Всего заработано
   - Выведено
   - Статистика по уровням (3 уровня)
   - Проценты по уровням
   - Реферальная ссылка

2. **Мои рефералы**
   - Список всех рефералов с разбивкой по уровням
   - Дата присоединения

3. **Мои начисления**
   - История транзакций начислений
   - Сумма, уровень, от кого, дата

4. **Вывод средств**
   - Запрос на вывод (если баланс >= min_withdrawal)
   - Подтверждение суммы и комиссии
   - История выводов со статусами

### 3.5. Управление выводами (админка)

**Статусы выводов:**
- `PENDING` - Ожидает рассмотрения
- `APPROVED` / `COMPLETED` - Выполнен
- `REJECTED` - Отклонен

**Действия администратора:**
1. Просмотр списка заявок на вывод
2. Фильтрация по статусу
3. Просмотр деталей (сумма, реквизиты, партнер)
4. Действия:
   - ✅ Выполнено (одобрить)
   - 💭 На рассмотрении
   - 🚫 Отказано (с комментарием)

### 3.6. Индивидуальные настройки партнера

Администратор может настроить индивидуальные параметры для конкретного партнера:

| Параметр | Описание |
|----------|----------|
| `use_global` | Использовать глобальные настройки |
| `accrual_strategy` | Условие начисления (первый платеж / каждый платеж) |
| `reward_type` | Тип награды (процент / фиксированная сумма) |
| `level1_percent` | Индивидуальный % для уровня 1 |
| `level2_percent` | Индивидуальный % для уровня 2 |
| `level3_percent` | Индивидуальный % для уровня 3 |

---

## 4. Детальный список отсутствующего функционала в altshop-panel

### 4.1. Админ-панель: Управление платежными шлюзами

**Приоритет: ВЫСОКИЙ**

```markdown
□ Список платежных шлюзов
  □ Таблица с колонками: Название, Тип, Статус, Валюта, Порядок
  □ Индикатор настроенности (все обязательные поля заполнены)
  □ Кнопки: Вкл/Выкл, Редактировать, Тест

□ Настройка шлюза (модальное окно/отдельная страница)
  □ Динамическая форма в зависимости от типа шлюза
  □ Поля для каждого шлюза (api_key, shop_id и т.д.)
  □ Маскирование секретных полей (SecretStr)
  □ Сохранение/Отмена

□ Управление порядком отображения
  □ Drag-and-drop или стрелки вверх/вниз
  □ Сохранение order_index

□ Управление валютой по умолчанию
  □ Выбор валюты для каждого шлюза

□ Информация о webhook
  □ Отображение URL для webhook
  □ Кнопка копирования

□ Тестирование шлюза
  □ Создание тестового платежа
  □ Проверка ответа от API
```

### 4.2. Админ-панель: Партнерская программа

**Приоритет: ВЫСОКИЙ**

```markdown
□ Главная страница партнерки
  □ Переключатель: Включена/Выключена
  □ Общая статистика: кол-во партнеров, рефералов, заявок на вывод
  □ Проценты по уровням (1, 2, 3)
  □ Налоговая ставка
  □ Минимальная сумма вывода
  □ Комиссии платежных систем

□ Настройка процентов по уровням
  □ Редактирование level1_percent, level2_percent, level3_percent
  □ Валидация 0-100%

□ Настройка налогов
  □ Редактирование tax_percent

□ Настройка комиссий ПС
  □ Таблица комиссий для каждого шлюза
  □ Редактирование каждой комиссии

□ Настройка минимального вывода
  □ Поле ввода суммы (в рублях, хранить в копейках)

□ Управление заявками на вывод
  □ Таблица заявок с фильтрами (PENDING, APPROVED, REJECTED)
  □ Колонки: ID, Партнер, Сумма, Статус, Дата
  □ Действия: Просмотр, Одобрить, Отклонить

□ Детали заявки на вывод
  □ Информация о партнере
  □ Сумма, статус, даты
  □ Реквизиты для вывода
  □ Комментарий администратора
  □ Кнопки: Выполнено, На рассмотрении, Отказать
```

### 4.3. Админ-панель: Управление партнерами (в карточке пользователя)

**Приоритет: СРЕДНИЙ**

```markdown
□ Секция "Партнерка" в профиле пользователя
  □ Статус: Нет партнерки / Активна / Неактивна
  □ Кнопка "Выдать партнерку" (если нет)
  □ Кнопка "Активировать/Деактивировать" (если есть)
  □ Кнопка "Удалить партнерку"

□ Статистика партнера
  □ Баланс
  □ Всего заработано
  □ Количество приглашенных рефералов
  □ Начисления по уровням

□ Изменение баланса партнера
  □ Добавление/снятие средств

□ Индивидуальные настройки партнера
  □ Переключатель: Глобальные / Индивидуальные
  □ Условие начисления (первый/каждый платеж)
  □ Тип награды (процент/фиксированная сумма)
  □ Проценты по уровням (если индивидуально)

□ История выводов пользователя
  □ Список заявок со статусами
```

### 4.4. Клиентский интерфейс: Партнерская программа

**Приоритет: СРЕДНИЙ**

```markdown
□ Условное отображение меню
  □ Показывать "Партнерка" только если is_partner = true

□ Главная страница партнерки
  □ Баланс (доступно к выводу)
  □ Всего заработано / Выведено
  □ Статистика по 3 уровням рефералов
  □ Отображение процентов по уровням
  □ Реферальная ссылка с кнопкой копирования

□ Страница "Мои рефералы"
  □ Список рефералов с уровнями
  □ Даты присоединения

□ Страница "Мои начисления"
  □ История начислений
  □ Сумма, уровень, источник

□ Страница "Вывод средств"
  □ Отображение баланса и минималки
  □ Форма запроса вывода (если баланс >= min)
  □ Подтверждение с указанием комиссии

□ История выводов
  □ Список с статусами (PENDING, APPROVED, REJECTED)
  □ Даты и суммы
```

### 4.5. Система локализации

**Приоритет: СРЕДНИЙ**

```markdown
□ Интеграция Fluent/fluentogram или аналога
  □ Установка библиотеки для React
  □ Настройка провайдера

□ Перенос переводов из FTL
  □ Конвертация или совместимость с FTL
  □ Или перенос в JSON-структуру

□ Поддержка двух языков: RU, EN
  □ Переключатель языка
  □ Сохранение предпочтения

□ Перевод всех UI-элементов
  □ Кнопки
  □ Заголовки
  □ Сообщения
  □ Уведомления
```

### 4.6. Webhook обработчики для платежей

**Приоритет: ВЫСОКИЙ**

```markdown
□ API endpoints для webhook
  □ POST /api/webhook/payment/:gatewayType
  □ Поддержка всех 7 шлюзов

□ Обработка webhook
  □ Верификация подписи/IP
  □ Обновление статуса транзакции
  □ Начисление партнерских вознаграждений
  □ Отправка уведомлений
```

---

## 5. Приоритеты внедрения

### Этап 1: Критический (Блокирует запуск)
**ETA: 1-2 недели**

1. **Платежные шлюзы - Webhook API**
   - Без webhook платежи не будут завершаться
   - Обязательно для всех активных шлюзов

2. **Платежные шлюзы - Базовый CRUD**
   - Список шлюзов
   - Включение/выключение
   - Базовая настройка ключей

### Этап 2: Высокий приоритет
**ETA: 2-3 недели**

3. **Партнерская программа - Бэкенд**
   - Таблицы БД для партнеров
   - API для начислений
   - Обработка выводов

4. **Партнерская программа - Админка**
   - Настройки программы
   - Управление выводами

5. **Платежные шлюзы - Полный функционал**
   - Порядок отображения
   - Валюта по умолчанию
   - Тестирование

### Этап 3: Средний приоритет
**ETA: 3-4 недели**

6. **Партнерская программа - Клиентский интерфейс**
   - Условное меню
   - Страница статистики
   - Запросы на вывод

7. **Локализация**
   - Перенос переводов
   - Переключатель языка

### Этап 4: Низкий приоритет
**ETA: 4-6 недель**

8. **Дополнительные шлюзы**
   - Yoomoney, Cryptomus, Robokassa

9. **Улучшения**
   - Drag-and-drop для порядка шлюзов
   - Расширенная статистика
   - Экспорт отчетов

---

## 6. Технические заметки

### Модели данных (для backend)

```typescript
// Payment Gateway
interface PaymentGateway {
  id: number;
  orderIndex: number;
  type: 'CRYPTOPAY' | 'HELEKET' | 'PAL24' | 'PLATEGA' | 'WATA' | 'YOOKASSA' | 'TELEGRAM_STARS';
  currency: string;
  isActive: boolean;
  settings: GatewaySettings;
}

// Partner
interface Partner {
  id: string;
  userId: string;
  telegramId: string;
  isActive: boolean;
  balance: number; // в копейках
  totalEarned: number;
  createdAt: Date;
}

// Partner Referral
interface PartnerReferral {
  id: string;
  partnerId: string;
  referralUserId: string;
  level: 1 | 2 | 3;
  totalEarned: number;
  createdAt: Date;
}

// Partner Withdrawal
interface PartnerWithdrawal {
  id: string;
  partnerId: string;
  amount: number; // в копейках
  status: 'PENDING' | 'APPROVED' | 'REJECTED';
  paymentDetails?: string;
  adminComment?: string;
  createdAt: Date;
  processedAt?: Date;
}

// Partner Settings
interface PartnerSettings {
  enabled: boolean;
  level1Percent: number;
  level2Percent: number;
  level3Percent: number;
  taxPercent: number;
  minWithdrawalAmount: number; // в копейках
  gatewayFees: {
    yookassa: number;
    telegramStars: number;
    cryptopay: number;
    heleket: number;
    pal24: number;
    wata: number;
    platega: number;
  };
}
```

### API Endpoints (рекомендуемые)

```
# Payment Gateways
GET    /api/admin/gateways
PUT    /api/admin/gateways/:id
PUT    /api/admin/gateways/:id/toggle
POST   /api/admin/gateways/:id/test
POST   /webhook/payment/:gatewayType

# Partner Admin
GET    /api/admin/partner/settings
PUT    /api/admin/partner/settings
GET    /api/admin/partner/withdrawals
PUT    /api/admin/partner/withdrawals/:id/approve
PUT    /api/admin/partner/withdrawals/:id/reject
POST   /api/admin/users/:id/partner

# Partner Client
GET    /api/client/partner/me
GET    /api/client/partner/referrals
GET    /api/client/partner/earnings
POST   /api/client/partner/withdrawals
GET    /api/client/partner/withdrawals
```

---

## Заключение

Altshop-panel требует значительного дополнения функционала для достижения паритета с Telegram-ботом. Ключевые функции, требующие внедрения:

1. **Платежные шлюзы** - архитектурно сложная часть с webhook-обработкой
2. **Партнерская программа** - многоуровневая система с выводами средств
3. **Локализация** - перенос ~6000 строк переводов

Начать следует с webhook для платежей (блокер) и базового CRUD шлюзов, затем перейти к партнерской программе.
