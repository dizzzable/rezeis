# Proxy configs + scripts run in Linux containers — force LF so a Windows
# checkout (autocrlf) can't break them.
*.sh        text eol=lf
*.conf      text eol=lf
Caddyfile   text eol=lf
*.yml       text eol=lf
*.yaml      text eol=lf
*.example   text eol=lf
